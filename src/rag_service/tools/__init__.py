"""Agent/tool integrations callable from the query pipeline.

Reserved package so adding tools later (e.g. web search) is a drop-in, not a
structural change. See `tools/websearch/` for the first reserved slot.
"""
